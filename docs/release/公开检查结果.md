# 公开检查结果

- 检查日期：2026-07-22
- 应用版本：1.2.1
- Bundle ID：`com.arlo.codex-quota-menu`
- 支持架构：Apple Silicon `arm64` 与 Intel `x86_64`
- 最低系统：macOS 13
- 许可：PolyForm Noncommercial License 1.0.0

## 已验证

- 本地测试、应用自检、Universal 2 构建、临时签名与 `Info.plist` 校验通过。
- 已覆盖 rollout Token 事件中 `info = null` 的启动崩溃回归。
- 隐私扫描未发现凭据、本机绝对路径或已知私有用量样例。
- 应用自身不包含网络请求客户端、分析 SDK、遥测或崩溃上报 SDK。

## 权限与数据边界

- 应用读取本机 Codex 状态、会话索引、SQLite、数据库引用的 rollout 文件和本机 `app-server` 返回值。
- 辅助功能权限只用于读取 Codex 顶部当前可见的任务名称，不记录键盘输入，也不控制点击。
- 本地仅保存浮窗位置、缩放比例、边缘吸附状态和最近一次历史 Token 总量。
- 显示数据不是 OpenAI 官方账单、额度保证、安全控制或合规结论。

## 发布边界

- 仓库公开源码，未经书面许可不得商业使用。
- 软件按现状提供，安装、权限调整、使用、修改、再分发及依赖显示结果所产生的风险由使用者自行承担。
- Codex Token Monitor 是独立社区项目，与 OpenAI 不存在隶属、认可、赞助或官方支持关系。
